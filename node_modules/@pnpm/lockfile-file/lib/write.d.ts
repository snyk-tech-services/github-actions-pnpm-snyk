import { Lockfile } from '@pnpm/lockfile-types';
export declare function writeWantedLockfile(pkgPath: string, wantedLockfile: Lockfile, opts?: {
    forceSharedFormat?: boolean;
}): Promise<unknown>;
export declare function writeCurrentLockfile(pkgPath: string, currentLockfile: Lockfile, opts?: {
    forceSharedFormat?: boolean;
}): Promise<unknown>;
export default function writeLockfiles(pkgPath: string, wantedLockfile: Lockfile, currentLockfile: Lockfile, opts?: {
    forceSharedFormat?: boolean;
}): Promise<[unknown, void]>;
